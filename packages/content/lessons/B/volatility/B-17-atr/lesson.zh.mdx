# 平均真实波幅 (ATR)

每一个波动率指标本质上都在回答同一个问题——**这个市场平均会动多大？** 1978 年 J. Welles Wilder 给出的答案叫 **平均真实波幅 (ATR)**。它不告诉你方向，只告诉你"噪声地板"——相邻两根 K 线之间一般会走多远——**单位就是价格本身**。

这一课你会学到：怎么手算 ATR、为什么它比朴素的"最高减最低"更可靠、以及怎样用 ATR 控制仓位和挂止损。

## 真实波幅 (TR)：补上跳空缺口

最朴素的"日内振幅"就是 $H_t - L_t$，在 24 小时连续交易的市场里没问题。但只要市场要收盘——股票、期货——就有问题：SPY 昨天收 \$500、今天低开到 \$490（跳空 \$10），盘中再走 \$2 的小区间，朴素公式只算出 \$2，**\$10 的跳空被无声丢掉了**。

Wilder 的解法是 **真实波幅 (True Range)**——取三个候选值的最大值：

$$
\mathrm{TR}_t = \max\bigl(H_t - L_t,\; |H_t - C_{t-1}|,\; |L_t - C_{t-1}|\bigr)
$$

后两项把当前 K 线**锚到昨日收盘价**。如果隔夜跳空很大，$|H_t - C_{t-1}|$ 就会主导结果。TR 捕捉到了朴素振幅忽略的 **跳空风险 (gap risk)**。

## ATR：对 TR 的 Wilder 平滑

单根 TR 噪声很大，Wilder 把它做滑动平均：

$$
\mathrm{ATR}_t = \frac{(n-1)\,\mathrm{ATR}_{t-1} + \mathrm{TR}_t}{n}
$$

默认 $n = 14$。这等价于一个 $\alpha = 1/n$ 的指数移动平均，**不是**更常见的 $\alpha = 2/(n+1)$——更慢、更钝。种子值通常取前 $n$ 根 TR 的简单平均。

ATR **不是**年化、百分比、方向信号。它的单位就是价格。SPY 的 ATR=4.20 表示当前每根日 K 平均走 \$4.20。

<Chart symbol="SPY" height={360} intervalLabel="D" />

看着这张图，想象在每根收盘价上下各画一条宽 $\pm \mathrm{ATR}$ 的带子。恐慌段它会变宽，平稳段它会变窄——这就是你正在度量的"噪声地板"。

## 怎么用：仓位规模与止损

ATR 在专业交易里有两个非常常见的用法，几乎可以说是整个行业的"母方"。

**波动率目标化仓位 (volatility-targeted sizing)。** 先决定每笔交易愿意承担多少美元风险（比如 \$500），然后让仓位规模刚好满足"一倍 ATR 的不利波动 = 这个风险预算"：

$$
\text{shares} = \frac{\text{risk budget}}{k \cdot \mathrm{ATR}_t}
$$

常数 $k$ 表示你愿意承受几倍 ATR 的浮亏才止损（常见 $k=2$）。ATR 翻倍，仓位就减半——这正是让一个策略的**风险在不同行情下大致保持不变**的关键机制。

**基于 ATR 的止损位。** 不要把止损固定挂在"距离入场价的某个百分比"——那种止损完全不知道当前市场有多吵。改成入场价下方 $k \cdot \mathrm{ATR}$ 挂止损：平静时止损紧，狂躁时止损宽，避免被正常噪声扫掉。

第三个常见用法是 **状态过滤 (regime filter)**：只在当前 ATR 高于 60 日中位数时才接受突破信号，确保市场"活着"才追。

## 一个手算例子

假设 SPY 昨日收 500，今日 H=503、L=498。那么 $H - L = 5$，$|H - C_{t-1}| = 3$，$|L - C_{t-1}| = 2$，所以 $\mathrm{TR}_t = 5$。若昨日 $\mathrm{ATR} = 4.20$、$n=14$，新的 ATR：

$$
\mathrm{ATR}_t = \frac{13 \cdot 4.20 + 5}{14} \approx 4.26
$$

一根高 TR 的 K 线只"轻推"了均值，不会让它跳起来。这种粘性正是 ATR 的目的——基于它算出的仓位不会被一根噪声 K 线推得忽大忽小。

## 容易踩的坑

<Pitfall title="ATR 不预测方向">
新手最常见的错误是把 ATR 叠在 RSI / MACD 旁边读"买卖信号"。**ATR 上升的全部含义就是"K 线变大了"**，它完全不告诉你是在向上变大还是向下变大。一个稳健上涨趋势里 ATR 也可以缓慢上升；一段横盘里 ATR 突然抬升，往往是大行情之前的能量积聚。**用 ATR 给仓位称重，不要拿它择时。**
</Pitfall>

<KeyPoint title="带走这一句">
ATR 是**用价格单位度量的绝对噪声地板**。让仓位和止损跟着波动率呼吸——但永远不要把它当成方向信号。真实波幅 (TR) 之所以存在，就是为了不让朴素的"最高减最低"悄悄丢掉隔夜跳空风险。
</KeyPoint>

## 小测验

<Quiz
  id="B-17-atr-q1"
  question="为什么真实波幅 (TR) 要把"昨日收盘价"也参与计算，而不是只用今日的 H - L？"
  options={[
    '为了让公式看起来更复杂、更专业。',
    '因为朴素的最高减最低会忽略隔夜跳空带来的风险。',
    '因为 Wilder 想让 ATR 成为一个领先的方向性指标。',
    '因为昨日收盘价永远高于今日最低价。',
  ]}
  answer={1}
  explanation="后两项把今日 K 线锚定到昨日收盘价上，让向上或向下的跳空完全反映在 TR 里。否则一段平静日里 $10 的隔夜跳空会被记成一个微小的区间。"
/>

<Quiz
  id="B-17-atr-q2"
  question="风险预算保持每笔 $500，但 ATR 从 4 升到 8（翻倍）。在 k = 2 的波动率目标化仓位方案下，你的下单股数怎么变？"
  options={[
    '股数翻倍——波动越大机会越多。',
    '股数不变——ATR 与仓位无关。',
    '股数减半——同样的美元风险只够买一半的股数了。',
    '股数变成原来的四倍——按 ATR 的平方缩放。',
  ]}
  answer={2}
  explanation="股数 = 风险预算 / (k × ATR)。ATR 翻倍意味着分母翻倍，股数自然减半。这正是让单笔美元风险在不同波动环境下保持稳定的机制。"
/>
