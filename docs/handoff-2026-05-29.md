# Session handoff — 2026-05-29 (cloud → local Teleport)

This document is the bridge between the cloud Claude Code session that
built milestones M0–M9 + M8 + infra hardening, and the local session
that will pick up from here. Read this first.

## TL;DR

- **Branch:** `claude/quant-trading-platform-3RrRH`
- **HEAD:** `f20b8f4` — "A: complete Track A (A-04..A-10) — curriculum now 110/110 authored"
- **PR:** [#6](https://github.com/w-zzzz/hello-world/pull/6) — draft, **all 20 CI checks green** on the final commit
- **Curriculum:** **110 / 110 lessons** authored, all `mdxReady: true`, all bilingual zh/en
- **Working tree:** clean, fully in sync with `origin`
- **Nothing in flight.** No uncommitted work, no open agents, no pending integration steps

## State of the platform (after this session)

| Milestone | What | Status |
|---|---|---|
| M0 | Monorepo bootstrap | ✅ |
| M1 | Track A first 3 lessons + chart + i18n | ✅ |
| M2 | 5 indicators with Python↔TS parity gate | ✅ |
| M3 | Auth + DB + XP MVP | ✅ |
| M4 | AI tutor MVP (streaming + 3-layer cache + budget) | ✅ |
| M5 | Backtest backend + sandbox + tearsheet | ✅ |
| M5.1 | Ultra-review hardening (5 sandbox escapes closed, XSS, CJK tokens, schema drift) | ✅ |
| M6 | Strategy editor + Pyodide browser sandbox | ✅ |
| M7 | Tracks B + C (40 lessons) | ✅ |
| **M8** | **Gamification — achievements, streaks, leaderboard, unlock toasts** | ✅ (this session) |
| **infra** | **Pre-commit hook tolerates fresh worktrees + MDX JSX-attribute CI linter** | ✅ (this session) |
| **M9** | **Tracks D–H (59 lessons)** | ✅ (this session) |
| **A-tail** | **Track A A-04..A-10 (7 lessons)** | ✅ (this session) |
| M10 | AI tutor tool use + extended thinking | TODO |
| M11 | Polish (Lighthouse ≥95, axe, error/empty states, full cmd-K) | TODO |
| M12 | Launch checklist (legal, Sentry, runbook, status page) | TODO |

## What landed this cloud session (12 commits)

```
f20b8f4 A: complete Track A (A-04..A-10) — curriculum now 110/110 authored
8dac3c3 M9: integrate Tracks D–H content (59 lessons) — curriculum now 103/110 ready
99022d2 M9: ADR for Tracks D–H content build-out (~57 lessons)
f5deccc M8(lint): satisfy biome ci on M8 frontend components
bbbf28a M8(integration): wire frontend↔backend — achievements API + opt-in + unlock toast
3f8f2a3 M8(frontend): achievements grid + leaderboard + streak indicator + toast
971fbbb M8(backend): achievements + streak server-side + leaderboard endpoint
a1c78d8 infra(mdx): satisfy ruff on check-mdx-attrs.py (RUF001 + E501 + format)
3dd1595 infra(mdx): add JSX attribute linter to catch nested " before build
d0cd2f4 infra(husky): pre-commit tolerates fresh worktrees missing pkg/node_modules/biome
```

ADRs added/relevant:

- `docs/adr/M7-tracks-bc.md` (B+C content build-out)
- `docs/adr/M9-tracks-defgh.md` (D–H content build-out)
- `docs/reviews/ultra-review-m5.md` (consolidated review)

## Pickup snapshot (run these locally to verify a clean checkout)

```bash
git pull origin claude/quant-trading-platform-3RrRH
pnpm install
pnpm --filter @quant-academy/content test          # expect 15/15, 110 mdxReady
pnpm --filter web typecheck                        # expect 0 errors
pnpm exec biome ci .                               # expect "No fixes applied" + 0 errors
uv run ruff check . && uv run ruff format --check .  # expect "All checks passed!"
python3 scripts/check-mdx-attrs.py                 # expect "checked 220 MDX file(s) -- 0 issues"
QA_AUTH_MODE=dev pnpm --filter web build           # prerenders all 110 lessons
```

The web build in this cloud container needed `NODE_OPTIONS=--max-old-space-size=3072`
to dodge an OOM (1 GB container ceiling). Your local machine almost certainly
has plenty of headroom — drop the override.

## Cleanup you may want to do locally

1. **Scratch agent branches on origin.** During M7/M8/M9 each worktree-isolated
   agent pushed its own `worktree-agent-<id>` branch as a backup integration
   source. They've all been integrated into trunk via copy-from-worktree-path
   or cherry-pick, so the branches are dead weight. 16 of them on origin:

   ```bash
   git push origin --delete $(git branch -r | grep 'origin/worktree-agent-' | sed 's|origin/||' | xargs)
   ```

2. **Local agent worktrees.** In the cloud env, 56 worktrees under
   `.claude/worktrees/agent-*` were still locked by live harness PIDs at
   teardown. These are gitignored (`.claude/worktrees`) and won't follow
   the local clone — nothing to do locally.

3. **PR #6 — currently a draft.** Flip to ready-for-review when you want
   human review / merge. The diff is large (12 commits, 110 lessons,
   gamification stack, infra) — likely a one-shot squash-merge target
   rather than per-commit review.

## Open items / non-blockers

These are accurate but not on the critical path:

- **Track A meta.ts tag drift.** A few D-track agents used simpler tags
  (`['backtest','metrics']`) than the curriculum's per-lesson tags
  (`['backtest','risk-metrics']`). Cosmetic — the loader doesn't read meta.ts;
  `curriculum.ts` is authoritative. Won't affect anything visible. Easy fix
  if it bothers anyone: sync meta.ts → curriculum.ts.

- **Two quiz-perfection achievements deferred** (`first-quiz-perfect`,
  `ten-perfect-quizzes`). M3's schema records aggregate score 0–100, not
  (correct, total). Adding the column is its own follow-up migration.
  M8 ships 8/10 achievements; TODO comment in `packages/gamification/src/achievements.ts`.

- **Leaderboard opt-in is client-side only.** `PATCH /api/leaderboard/opt-in`
  validates auth and acks, but no `users.leaderboard_opt_in` column yet —
  preference lives in `localStorage`. Adding the column is part of the same
  follow-up as the quiz-perfection achievements.

- **M5 prod-only Layer-2 Docker `--runtime=runsc`.** Documented in M5/M5.1 ADRs;
  not enforced in CI because runsc isn't in the Actions runner. Operator
  responsibility at deploy time. (Layer-1 = qa_sandbox in-process restrictions
  IS enforced and tested.)

## Next-step menu (pick when you're back)

- **M10 — AI tutor tool use.** Add `compute_indicator`, `run_backtest`,
  `lookup_lesson` tools to the tutor; render returned charts inline in chat;
  opt-in extended thinking for "deep review" CTA. This is the
  highest-leverage remaining feature work.

- **M11 — Polish.** Lighthouse perf+a11y ≥ 95 across the now-large surface
  (landing, lessons, indicators, workshop, editor, profile, achievements,
  leaderboard); axe-core sweep; empty/error states everywhere; full cmd-K
  (search lessons, switch locale, toggle theme — currently a stub).

- **M12 — Launch checklist.** Legal pages (Terms / Privacy / Data License),
  Sentry source maps + PII scrubber, on-call runbook, status page,
  `release.yml`, data-refresh cron, BYO-LLM hook.

- **Track A meta.ts tag cleanup** — small, ~10 min if you care.

- **M8.1** — add `users.leaderboard_opt_in` + quiz score columns, ship the
  two deferred achievements.

## Key files to re-orient

- `docs/adr/` — every milestone has its sub-ADR documenting decisions
- `packages/content/src/curriculum.ts` — authoritative lesson catalog
  (110 entries, all `mdxReady: true`)
- `packages/content/lessons/<TrackId>/<module>/<lesson-id>/{meta.ts,lesson.{zh,en}.mdx}` — the 110 lesson units
- `scripts/check-mdx-attrs.py` — the infra fix that kept M9 from rerunning M7's nested-quote debug loop. Run before commit if you touch MDX.
- `.husky/pre-commit` — worktree-tolerant version; safe to use from any branch
- `packages/gamification/src/{achievements,evaluator,streak}.ts` — M8 backend logic, 65 unit tests
- `apps/web/components/achievement-toast.tsx` — fires on `qa:achievement-unlock` window CustomEvent dispatched by `mark-complete-button.tsx`

— end of cloud session, 2026-05-29
