// Empty stub. The real `server-only` package would throw at bundle time if
// imported into a client module; in node-side unit tests we want it to be a
// no-op so we can call into server modules directly.
export {};
